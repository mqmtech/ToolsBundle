; These parameters can be imported into other config files
; by enclosing the key with % (like %database_user%)
; Comments start with ';', as in php.ini
[parameters]
    
    field1=value1
    field2=value2
    field3=value3

;this line prevents from errors in values in last line parameters